# COMP3123 Frontend


## Setup
```bash
cd frontend
npm install
npm run dev
```

## Scripts
- `npm run dev` — start Vite dev server (defaults to http://localhost:3000)
- `npm run build` — production build
